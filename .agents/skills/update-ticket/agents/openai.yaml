interface:
  display_name: "Update Ticket"
  short_description: "Change a ticket status, checking matching worktrees"
  default_prompt: "Update the requested ticket status, checking a matching .worktrees checkout when present, refresh docs/tickets/INDEX.md, and commit the documentation changes."

policy:
  allow_implicit_invocation: false
