interface:
  display_name: "Create Tickets"
  short_description: "Generate development ticket files from requirements documents"
  default_prompt: "Read the requirements documents, break them into development tickets, write ticket files to docs/tickets/, and create an INDEX.md tracker."

policy:
  allow_implicit_invocation: false
